name: KindDefaults
cabal-version: >= 1.24
build-type: Simple
version: 1.0.0

library
  default-language: Haskell2010
  build-depends: base,
                 data-default,
                 ghc >= 8.10
  exposed-modules:
    KindDefaults.Plugin,
    KindDefaults
  ghc-options: -Wall

